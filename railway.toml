[build]
builder = "nixpacks"

[deploy]
startCommand = "cd src && python -m hitomi.server"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
